from fastapi import APIRouter

router = APIRouter(prefix="/admin", tags=["admin"])

# TODO: implementar en change us-007-admin
# GET /dashboard/metricas   (ADMIN)
# GET /stock                (ADMIN, STOCK)
# GET /usuarios             (ADMIN)
